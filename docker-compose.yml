# Version
version: '3.1'

# Setup
services:
  # PHP
  php:
    image: lando45/lando-web
    restart: always
    ports:
      - 8080:80
    volumes:
      - /home/lando/Training/lando-web:/var/www/html

